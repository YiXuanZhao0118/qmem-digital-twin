# CLAUDE.md — qmem-digital-twin

Project-specific guidance. Merge with the global guidelines.

## Authoritative docs — read these instead of grepping source

- For **architecture / concept questions**, read [`docs/introduce/README.md`](docs/introduce/README.md) (the index) first, then the relevant per-concept file. As of 2026-06-11 every file under `docs/introduce/` has been line-verified against the code — treat them as the **authoritative, current map** of the system.
- **Trust the docs for orientation.** Do NOT re-read source just to confirm what a doc already states — *unless you are about to change that area's code* (see "Read vs verify").
- **Auto-memory** (`MEMORY.md` + topic files) holds project state/decisions/gotchas — check it before assuming. Note: it is **machine-local and keyed to the launch cwd** (`~/.claude/projects/<cwd-hash>/memory/`), so it does **NOT** live in or travel with this repo, and it differs per cwd — launch Claude from a consistent directory or the memory won't load. Keep it updated as state changes.

## Locked rows — do NOT modify (human-confirmed complete)

A Kind, Asset3D, Device or Component row can carry `locked = true` (DB column, alembic 0112; Device 0123, Component 0128). It means a human has reviewed that row, confirmed it is **complete and correct**, and frozen it. Treat a locked row as off-limits:

- **Never edit, re-tune, "fix", or delete a locked Kind / Asset3D / Device / Component** — not its `default_params`, anchors, `wavelength_range_nm`, nothing. If your task seems to require changing a locked row, **stop and ask the user to unlock it first** (or to confirm the change); don't work around the lock.
- **How to check:** the flag is `kinds.locked` / `assets_3d.locked`, surfaced as `locked` on `KindOut` / `Asset3DV3Out` (GET `/api/kinds`, `/api/v3/assets3d`). The PHY Editor shows a 🔒 on each locked list row.
- **Enforcement is real, not advisory:** every write path (`PATCH /api/kinds/{id}`, `PUT /api/v3/assets3d/{key}`, `PUT /api/assets/{id}`, and the deletes) rejects any change to a locked row with **422** unless the request *only* toggles `locked` (i.e. unlock). Guard: `backend/app/lock_guard.py`. So a tool call that edits a locked row will fail — don't retry it, surface the lock to the user.
- Unlocking is a deliberate **human** action (click the lock icon in the PHY Editor). Do not unlock a row on your own initiative to push an edit through.

## Read vs verify (balance context cost against correctness)

This is the core rule for using docs efficiently:

- **Answering a question / orienting** → rely on `docs/introduce/` + `MEMORY.md`. Avoid broad grepping or reading large swaths of source. The docs exist precisely so you don't have to.
- **Making a CODE change** → verify the *specific* code you're touching against the actual source. Docs can lag the code by minutes.
- **High-risk changes** (DB schema / Alembic migrations, the render pipeline, the optical tracer / physics ops, anything cross-cutting) → **spot-verify against source even when the docs look right.** A stale doc leading to wrong code is a real, recurring failure mode — never suppress verification entirely here.
- If the user says "trust the docs, don't re-verify," honour it for questions; for code edits still apply the high-risk rule above and say so.

## Keeping docs trustworthy (this is what makes the rule above safe)

- **Every session that changes code MUST update the affected `docs/introduce/*.md` in the same change.** Code and its doc move together so the docs never drift. If you rename a field, change a behaviour, add/remove an endpoint, retire a file, etc. — fix the doc(s) that describe it in the same commit. A code commit that silently outdates a doc is a bug.
- **Cite `file:line` + invariants** in docs so future reads jump precisely instead of searching. Prefer "X lives in `path/file.ts:NN`; invariant: Y must hold" over vague prose. When you touch a doc, add/repair these anchors.
- **Split large docs + cross-link.** If a doc grows too large or covers multiple topics, split it into per-concept files and link them — exactly as `docs/introduce/` already does: an index `README.md` + single-topic files, each starting with `[← 文件索引](README.md)` and cross-linking siblings via `[other.md](other.md)`. Keep each file one topic.
- After adding/splitting/renaming docs: update the index `README.md` link list **and** run a link check (no broken `](*.md)` targets). Update the `MEMORY.md` line + the relevant memory file if the doc set's shape changed.

## Current model (quick pointers — details in docs/introduce/)

- **Optical engine**: live = `backend/app/optical/anchor_tracer.py` (anchor-based) + `solver.py` (`solve_anchor_scene`), endpoint `/api/v3/solver`, DB→slots via `db_scene_loader.load_anchor_scene_from_db`. Dispatch via `anchor_ops/<kind>.py`. Only `PRIMARY_ANCHOR_IDS` (`intercept_in/out/face`, `interaction_center`, `optical_center`, `fiber_in`) are hit-tested. faces/transitions retired (migration 0106) → `anchors[]`. Face-based `ray_tracer.py` is legacy.
  - **The fibre vocabulary (alembic `0133`-`0135`)**, read as "where the FIBRE is", not which way light goes: **`fiber_in`** = goes into an instrument (chassis socket, female, on the device asset — RXM15EF); **`fiber_out`** = comes out of its connector (mating face, male, on a `fiber_connector` asset); **`fiber_root`** = anchored into the cable (spline junction). **Only `fiber_in` is in `PRIMARY_ANCHOR_IDS`** — the connector is passthrough and a cable's coupling is traced on the synthesized `intercept_in/out` derived from `fiber_out`. Coax keeps `connect_in`/`connect_out` for the last two; one lookup takes both spellings (`utils/connectorAnchors.ts`, `db_scene_loader._find_anchor`). `connectorType` is gendered and `isFiberPortConnectorType` is female-only — that plus `isFiberReceptacleAnchor` keying on `fiber_in` ALONE (never a `fiber_*` prefix) is what stops two patch cables being offered as pluggable into each other.
  - **Fiber coupling is a synthesized slot, not a static asset**: a connector-component fiber binds two `fiber_connector` assets (passthrough; `connect_in/out` aren't primary), so the loader synthesizes a `fiber`-kind slot (`_synth_fiber_slot`) — `intercept_in/out` pose from the fiber PhysicsElement's `kindParams.endA/endB` (what Align writes), while the optical-face offset + hit aperture come from the connector asset's `connect_in` (`_connector_tip_and_aperture`). Editing `connect_in` on the asset moves the coupling face / waist. Fiber **align** (`sceneStore.resolveEffectiveFiberNodes` + `syncFiberEndpointToKindParams`) reads/writes those same kindParams. Details in [`docs/introduce/fiber.md`](docs/introduce/fiber.md).
  - **Plugging a patch cable into an instrument (2026-08-21; reshaped by alembic `0133`, 2026-08-23)**: a fibre end links to a **bulkhead** — a `fiber_in` / `fiber_out` anchor declaring a **female** fibre `connectorType` (`fc_pc_female` / `fc_apc_female`), the exact pattern an RF port uses — via `SceneObject.properties.fiberEndpoints`, the optical twin of `rfCableEndpoints`. A bulkhead is NOT an `intercept_*` wearing a connector: an intercept is a face you can align a beam onto, a socket is not. `*_male` is a cable or pigtail plug and is never a port — `isFiberPortConnectorType` is female-only, because `collectFiberPortsLab` filters on it alone and would otherwise let two cables plug into each other. A **pigtail** (the EOSpace EOM) is the other thing entirely and keeps `intercept_*` + a bound connector; see [`docs/introduce/component.md`](docs/introduce/component.md). Two traps: the mating is **not** antiparallel (an optical anchor's axisX is a propagation direction, so End A faces −axisX and End B +axisX), and the link **must** be written through to `kindParams.endA/endB` because the solver reads the pose, not the link. The mated face sits `FIBER_MATING_GAP_MM` (10 µm) short of the plane — a ray emitted exactly on the receiving plane is dropped by `nearest_anchor_hit`'s `t_min`. A `fiber_connector` sub-component under a device is render-only and couples nothing.
- **Rendering**: unified onto the ComponentBinding tree (`bindingRendererGate.ts`, `shouldRenderViaBindings` always true). Legacy single-asset `loadAssetObject` dispatch is dead code; per-instance fiber/rf_cable/isolator state forwarding through the tree is still TODO.
- **Classification (two separate axes)**: **category ← component kind** (`Component.kind_id` → plugin `assetCategory`); **domain ← asset kind** (`Asset3D.kind_id` → `kind.domains` ∈ {optical, rf, mechanical}). Decoupled — `physicsCapabilities` no longer drives either.
- **Multiphysics**: Lab is the only top-level module (Optics/Electronics/EM removed 2026-06-10, migration 0109); Magnetics is a Lab overlay.
- **Alembic head**: `0137_laser_source_fiber_bulkhead` (the `laser_source` kind gains the optional **`fiber_in`** role — the same bulkhead 0133 gave `detector` — so a fibre-coupled source can declare the socket its patch cable plugs into instead of faking it with an `intercept_out` wearing a connector. **A fibre-coupled source must carry BOTH anchors, coincident on the bore face**: `emit_anchor_source_rays` seeds the trace from `intercept_out` and no other id, so the bulkhead alone emits nothing and reports no error. Safe because the ray starts on its own `fiber_in` plane → `t = 0` → dropped by `t_min = 1e-9`; pinned by `backend/tests/optical/test_fiber_coupled_laser_source.py`. First user: the `fiber_checker` device/asset (handheld 650 nm VFL). **Writes the `locked` `laser_source` row** with the user's explicit authorisation, lock left ON. See the port section of [`docs/introduce/fiber.md`](docs/introduce/fiber.md)). Before it, `0136_kinds_insert_missing` (inserts the four plugin-backed `kinds` rows that had never existed — `fiber`, `fiber_coupler`, `glan_polarizer`, `rf_cable`. 0126 was credited with this and its insert never took; `test_every_plugin_has_a_row` had been red continuously, and **9 components referenced `fiber` / 3 referenced `rf_cable` with no row to point at** (no FK on `components.kind_id`). Generic — inserts whatever the manifest has and the table lacks. Backend suite is now fully green and `audit_kind_param_drift.py` reports `clean: 31/31, IN SYNC`). Before it, `0135_fiber_conn_anchor_rename` (the fibre connector's `connect_out` / `connect_in` become **`fiber_root` / `fiber_out`**, so the fibre vocabulary is one scheme — `fiber_root` → `fiber_out` → `fiber_in` — instead of three, and two anchors a millimetre apart are no longer both called "in". Coax keeps `connect_*`; one lookup takes both spellings. **`fiber_out` leaves `PRIMARY_ANCHOR_IDS`** — the id now means a connector's mating face, which must stay passthrough. Trace-identical; writes the same four `locked` rows as 0134, locks left ON). Before it, `0134_fiber_connector_male_types` (the MALE half of the fibre vocabulary lands on the ferrules: every `fiber_connector` device + asset gets `connect_in.connectorType` = `fc_pc_male` / `fc_apc_male`, derived per row from `default_params.polish` (or an unambiguous `_pc` / `_apc` token, which is how the `devices` rows resolve — they carry `polish: null`). `connect_out` stays null; it is the spline junction and mates with nothing. No physics change — `connect_*` are not primary anchors and coupling reads the connector *params* — but it completes the plug/socket relation that keeps cable ends out of the port list. **Writes four `locked` rows with the user's explicit authorisation, leaving the locks ON**; see the locked-rows note in [`docs/introduce/migrations.md`](docs/introduce/migrations.md)). Before it, `0133_fiber_bulkhead_anchor` (a fibre **bulkhead** stops being an `intercept_in` wearing a connector and gets its own anchor, `fiber_in` / `fiber_out`, added to `PRIMARY_ANCHOR_IDS`; the fibre `connectorType` vocabulary becomes **gendered** — `fc_pc_female` / `fc_apc_female` for a chassis socket, `*_male` for a cable or pigtail plug, and `isFiberPortConnectorType` is female-only so two patch cables can never be plugged into each other. The `detector` kind gains the optional `fiber_in` role and `intercept_in` drops to `min: 0` (the free-space and fibre-coupled builds are mutually exclusive), and the `rxm15ef` device / `rxm15ef_step` asset / the `fiberEndpoints` links pointing at them are moved over. Verified trace-identical against the live scene. The `*_male` half followed in 0134. See the port section of [`docs/introduce/fiber.md`](docs/introduce/fiber.md) and the pigtail-vs-bulkhead table in [`docs/introduce/component.md`](docs/introduce/component.md)). Before it, `0132_eom_per_end_align` (the `eom` kind's `alignVariant` drops to `"none"`: a fibre-pigtailed modulator aligns per END — each port connector snaps to its own beam / receptacle via an `ObjectBinding` delta while the housing stays put — instead of as one rigid optic. The same `npm run export:kinds` also drags `backend/data/kinds.json` back up to date, since it had been left at the 2026-08-19 build while 0129–0131 moved the DB; see the align section of [`docs/introduce/component.md`](docs/introduce/component.md)). Before it, `0131_detector_photoreceiver` (the `detector` kind stops being optics-only — an optional `rf_out` role plus the four photoreceiver specs, so a fibre-in / electrical-out receiver like the Thorlabs RXM15EF is representable; the `rxm15ef` device is its first user, see [`docs/introduce/kinds.md`](docs/introduce/kinds.md)). Before it, `0130_eom_knobs_polarization` (the `eom` kind's two drive knobs become real `stateParamKeys` and a guided modulator gains its single-polarization behaviour; **PM fibre also became polarization-aware in the same change** — `_synth_fiber_slot` now puts the synthesized anchor's axisY on the connector key and `fiber_anchor_op` resolves the Jones vector against it, see [`docs/introduce/fiber.md`](docs/introduce/fiber.md)). Before it, `0129_eom_mz_params` (a one-row re-sync of the `eom` kind after the plugin gained the Mach-Zehnder params + the optional `rf_in` role — the EOM op does real intensity modulation and a fibre-pigtailed exit, see [`docs/introduce/optics.md`](docs/introduce/optics.md)). Before it, `0128_component_locked` (Component gets a real `locked` column + the shared `lock_guard`, replacing a `properties['locked']` JSONB flag that guarded delete only with a 409; the AI rule above now covers **Component** too). Before it, `0127_aom_acoustic_axis_anchor` (backfills the `acoustic_axis` anchor onto every `aom` asset — the direction's single source of truth since 2026-06-03, but the draining migration its two resolvers point at was never written, so the catalog's one AOM ran on the legacy param fallback; see [`docs/aom-model.md`](docs/aom-model.md)). Before it, `0126_kinds_manifest_resync` (**the `kinds` table must equal `backend/data/kinds.json`** — 0086 backfilled it once and nothing re-ran it, so 29 of the 31 plugin-backed rows had fossilised (only `dichroic_mirror` and `mirror` were clean); the invariant is now pinned by `backend/tests/test_kind_manifest_sync.py` and reportable via `backend/scripts/audit_kind_param_drift.py`. **Built-in kinds are edited in `frontend/src/kinds/<kind>/index.ts` + `npm run export:kinds` + a resync migration, NOT in the Kinds editor** — see [`docs/introduce/kinds.md`](docs/introduce/kinds.md)). Then `0123_devices` (the device registry as a DB table — devices left `frontend/src/devices/*.ts` so the PHY Editor's **DEVICE** section can create and correct them; `/api/devices`, seeded from `0123_devices_seed.json`) and `0122_asset_lods` (the LOD sidecar table — one row per (asset, level); kept off `assets_3d` because locked rows would 422, see [`docs/introduce/rendering.md`](docs/introduce/rendering.md)). Still load-bearing: `0113_param_ownership_tunable` (per-instance editing gated by `assets_3d.tunable_params`, values in `objects.dynamic_sources`; `objects.param_overrides` dropped). Stack ports: frontend 5173 / backend 8010 / Postgres 55432.
  - ⚠️ **0126, 0127, 0134, 0135 and 0137 all write `locked` rows**, which the rule above forbids. All were done with the user's explicit authorisation and are argued for in [`docs/introduce/kinds.md`](docs/introduce/kinds.md) + [`docs/introduce/migrations.md`](docs/introduce/migrations.md). Note the general lesson: `lock_guard` is **API-layer only**, so a migration bypasses it silently — the lock cannot stop you, only the rule can.
